/**
 * A binary search tree in TypeScript.
 * Keeps references to two child nodes in a hierarchical structure. The left
 * child must have a value that is smaller than or equal to its parent, while
 * the right child must have a greater value.
 * @class BinarySearchTree
 * @template T
 */
class BinarySearchTree<T> {

    /**
     * The data that this node stores.
     * @property value
     * @type {T}
     */
    // value: T;
}

export default BinarySearchTree;